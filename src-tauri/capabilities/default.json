{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the RuneChat desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-spawn"]
}
